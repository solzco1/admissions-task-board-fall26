# Legacy admissions Express task board API
#
# Original take-home: miniature task board with GET/POST/PATCH/DELETE /api/tasks
# Preserved as the foundation extended by the Cohort PM Next.js app.

## Run locally (legacy)

```bash
node legacy/src/index.js
```

## Original tests

The admissions starter tests lived in `legacy/tests/`. The current `npm test` runs
unit tests for the shared task status utilities in `lib/tasks.js`.
